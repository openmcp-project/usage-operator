# Track: nothing changed — existing ongoing tracking with same trait values
apiVersion: usage.open-control-plane.io/v1alpha1
kind: UsageServiceConfig
metadata:
  name: test-06
spec:
  resourcesToTrack:
  - group: apps
    version: v1
    kind: Deployment
    resourceUsagePeriod: 1h
    traits:
      replicas:
        path: .resource.spec.replicas
