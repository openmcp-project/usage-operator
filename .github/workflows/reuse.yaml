name: REUSE Compliance Check

on: [push, pull_request]

permissions:
  contents: read

jobs:
  run_reuse:
    uses: openmcp-project/build/.github/workflows/reuse.lib.yaml@dcab75e52b34789ede669947300504d208d06ae3 # main
    secrets: inherit
