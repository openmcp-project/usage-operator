---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  labels:
    openmcp.cloud/cluster: platform
  name: projectworkspaceconfigs.core.openmcp.cloud
spec:
  group: core.openmcp.cloud
  names:
    kind: ProjectWorkspaceConfig
    listKind: ProjectWorkspaceConfigList
    plural: projectworkspaceconfigs
    shortNames:
    - pwcfg
    singular: projectworkspaceconfig
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ProjectWorkspaceConfig is the Schema for the ProjectWorkspaceConfigs
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProjectWorkspaceConfigSpec defines the desired state of ProjectWorkspaceConfig
            properties:
              memberOverrides:
                description: |-
                  MemberOverrides allows to specify users and groups which should have admin permissions to projects and workspaces.
                  This basically disables the 'you must be admin of a project/workspace in order to modify it' check.
                  Leave empty to disable.
                items:
                  properties:
                    kind:
                      description: Kind of object being referenced. Can be "User",
                        "Group", or "ServiceAccount".
                      enum:
                      - User
                      - Group
                      - ServiceAccount
                      type: string
                    name:
                      description: Name of the object being referenced.
                      type: string
                    namespace:
                      description: Namespace of the referenced object. Required if
                        Kind is "ServiceAccount". Must not be specified if Kind is
                        "User" or "Group".
                      type: string
                    resources:
                      description: Resources defines an optional list of projects/workspaces
                        that this override applies to.
                      items:
                        properties:
                          kind:
                            enum:
                            - project
                            - workspace
                            - Project
                            - Workspace
                            type: string
                          name:
                            description: Name of the object being referenced.
                            type: string
                        required:
                        - kind
                        - name
                        type: object
                      type: array
                    roles:
                      description: Roles defines a list of roles that this override
                        subject should have.
                      items:
                        enum:
                        - admin
                        - view
                        type: string
                      type: array
                  required:
                  - kind
                  - name
                  - roles
                  type: object
                  x-kubernetes-validations:
                  - message: Namespace must not be specified if Kind is User or Group
                    rule: self.kind == 'ServiceAccount' || !has(self.__namespace__)
                  - message: Namespace is required for ServiceAccount
                    rule: self.kind != 'ServiceAccount' || has(self.__namespace__)
                type: array
              project:
                description: ProjectConfig contains the configuration for projects.
                properties:
                  additionalPermissions:
                    additionalProperties:
                      items:
                        description: |-
                          PolicyRule holds information that describes a policy rule, but does not contain information
                          about who the rule applies to or which namespace the rule applies to.
                        properties:
                          apiGroups:
                            description: |-
                              APIGroups is the name of the APIGroup that contains the resources.  If multiple API groups are specified, any action requested against one of
                              the enumerated resources in any API group will be allowed. "" represents the core API group and "*" represents all API groups.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                          nonResourceURLs:
                            description: |-
                              NonResourceURLs is a set of partial urls that a user should have access to.  *s are allowed, but only as the full, final step in the path
                              Since non-resource URLs are not namespaced, this field is only applicable for ClusterRoles referenced from a ClusterRoleBinding.
                              Rules can either apply to API resources (such as "pods" or "secrets") or non-resource URL paths (such as "/api"),  but not both.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                          resourceNames:
                            description: ResourceNames is an optional white list of
                              names that the rule applies to.  An empty set means
                              that everything is allowed.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                          resources:
                            description: Resources is a list of resources this rule
                              applies to. '*' represents all resources.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                          verbs:
                            description: Verbs is a list of Verbs that apply to ALL
                              the ResourceKinds contained in this rule. '*' represents
                              all verbs.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                        required:
                        - verbs
                        type: object
                      type: array
                    description: AdditionalPermissions defines additional permissions
                      users should have in a project, depending on their role.
                    type: object
                  resourcesBlockingDeletion:
                    items:
                      description: |-
                        GroupVersionKind unambiguously identifies a kind.  It doesn't anonymously include GroupVersion
                        to avoid automatic coercion.  It doesn't use a GroupVersion to avoid custom marshalling
                      properties:
                        group:
                          type: string
                        kind:
                          type: string
                        version:
                          type: string
                      required:
                      - group
                      - kind
                      - version
                      type: object
                    type: array
                type: object
              webhook:
                description: Webhook contains the configuration for the webhooks.
                properties:
                  disabled:
                    description: Disabled specifies whether the webhooks should be
                      disabled.
                    type: boolean
                type: object
              workspace:
                description: WorkspaceConfig contains the configuration for workspaces.
                properties:
                  additionalPermissions:
                    additionalProperties:
                      items:
                        description: |-
                          PolicyRule holds information that describes a policy rule, but does not contain information
                          about who the rule applies to or which namespace the rule applies to.
                        properties:
                          apiGroups:
                            description: |-
                              APIGroups is the name of the APIGroup that contains the resources.  If multiple API groups are specified, any action requested against one of
                              the enumerated resources in any API group will be allowed. "" represents the core API group and "*" represents all API groups.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                          nonResourceURLs:
                            description: |-
                              NonResourceURLs is a set of partial urls that a user should have access to.  *s are allowed, but only as the full, final step in the path
                              Since non-resource URLs are not namespaced, this field is only applicable for ClusterRoles referenced from a ClusterRoleBinding.
                              Rules can either apply to API resources (such as "pods" or "secrets") or non-resource URL paths (such as "/api"),  but not both.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                          resourceNames:
                            description: ResourceNames is an optional white list of
                              names that the rule applies to.  An empty set means
                              that everything is allowed.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                          resources:
                            description: Resources is a list of resources this rule
                              applies to. '*' represents all resources.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                          verbs:
                            description: Verbs is a list of Verbs that apply to ALL
                              the ResourceKinds contained in this rule. '*' represents
                              all verbs.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                        required:
                        - verbs
                        type: object
                      type: array
                    description: AdditionalPermissions defines additional permissions
                      users should have in a workspace, depending on their role.
                    type: object
                  resourcesBlockingDeletion:
                    items:
                      description: |-
                        GroupVersionKind unambiguously identifies a kind.  It doesn't anonymously include GroupVersion
                        to avoid automatic coercion.  It doesn't use a GroupVersion to avoid custom marshalling
                      properties:
                        group:
                          type: string
                        kind:
                          type: string
                        version:
                          type: string
                      required:
                      - group
                      - kind
                      - version
                      type: object
                    type: array
                type: object
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
